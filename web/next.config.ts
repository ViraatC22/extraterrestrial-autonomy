import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev-mode badge sits over the presentation-mode telemetry strip.
  devIndicators: false,
};

export default nextConfig;
